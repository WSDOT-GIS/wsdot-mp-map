{
  "name": "wsdot-mp-map",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "prepare": "npm run build",
    "preview": "npm run build && vite preview",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "leaflet": "^1.9.3",
    "typescript": "^4.9.4",
    "vite": "^4.0.4",
    "vitest": "^0.26.3"
  }
}
